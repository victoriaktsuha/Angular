export class Ingredient {
  constructor(public name: string, public amount: number) {}
  // we can declare the properties like this, directly in the constructor parameters, a short-way to do the same thing for a model
}
